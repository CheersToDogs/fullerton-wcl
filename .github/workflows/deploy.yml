name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deployed
        run: echo "Deployed $(date) - ${{ github.sha }}"
